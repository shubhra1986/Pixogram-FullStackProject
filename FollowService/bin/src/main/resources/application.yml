server:
  port: 8023
spring:
  h2:
    console:
      path: /console
      enabled: true
      settings:
        web-allow-others: true
  application:
    name: follow-service
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

   

